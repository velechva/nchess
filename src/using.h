#ifndef NCHESS_USING_H
#define NCHESS_USING_H

#include <utility>
#include <algorithm>
#include <optional>

using   std::pair,
        std::max,
        std::min,
        std::optional,
        std::nullopt,
        std::vector;

#endif